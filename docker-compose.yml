services:
  web:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - DEBUG=True
    command: daphne -b 0.0.0.0 -p 8000 vic_os_project.asgi:application
